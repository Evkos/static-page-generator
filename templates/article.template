<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    {{meta:title}}
    {{meta:description}}
</head>
<body>
    <h1>{{title}}</h1>
    <article>{{content}}</article>
    {{image:featuredImage}}
    {{thumbnail:featuredImage}}
</body>
</html>