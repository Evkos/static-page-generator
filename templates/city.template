CITY TEMPLATE
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{title}}</title>
    {{meta}}
</head>
<body>
    <h1>{{title}}</h1>
    <article>{{content}}</article>
    {{image:featuredImage}}
    {{thumbnail:featuredImage}}
    {{image:additionalImage}}
    {{thumbnail:additionalImage}}
    {{image:bonusImage}}
    {{thumbnail:bonusImage}}
</body>
</html>